{{! src/views/admin/condiciones/form.hbs }}
<div class="container-fluid py-4">
    <div class="mb-4">
        <a href="/admin/condiciones" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left"></i> Volver
        </a>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-tag"></i> {{title}}
                    </h5>
                </div>
                <div class="card-body">
                    {{#if error}}
                    <div class="alert alert-danger">{{error}}</div>
                    {{/if}}

                    <form method="{{method}}" action="{{action}}">

                        <div class="mb-3">
                            <label for="codigo" class="form-label">Código *</label>
                            <input type="text" class="form-control" id="codigo" name="codigo"
                                value="{{condicion.codigo}}" placeholder="ej: COND-19" required>
                            <small class="form-text text-muted">Código único de la condición</small>
                        </div>

                        <div class="mb-3">
                            <label for="nombre" class="form-label">Nombre *</label>
                            <input type="text" class="form-control" id="nombre" name="nombre"
                                value="{{condicion.nombre}}" placeholder="ej: FARMACIA 5% - ALIMENTO 9.09%" required>
                        </div>

                        <div class="mb-3">
                            <label for="descripcion" class="form-label">Descripción</label>
                            <textarea class="form-control" id="descripcion" name="descripcion" rows="3"
                                placeholder="Descripción detallada de la condición comercial">{{condicion.descripcion}}</textarea>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="vigencia_desde" class="form-label">Vigencia Desde</label>
                                <input type="date" class="form-control" id="vigencia_desde" name="vigencia_desde"
                                    value="{{formatDateInput condicion.vigencia_desde}}">
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="vigencia_hasta" class="form-label">Vigencia Hasta</label>
                                <input type="date" class="form-control" id="vigencia_hasta" name="vigencia_hasta"
                                    value="{{formatDateInput condicion.vigencia_hasta}}">
                            </div>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save"></i> Guardar Condición
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            {{#if condicion.id}}
            <div class="card shadow-sm mt-4">
                <div class="card-header bg-secondary text-white d-flex justify-content-between align-items-center">
                    <h6 class="mb-0">
                        <i class="bi bi-list-check"></i> Reglas de Descuento ({{reglas.length}})
                    </h6>
                    <button type="button" class="btn btn-light btn-sm" data-bs-toggle="modal"
                        data-bs-target="#modalNuevaRegla">
                        <i class="bi bi-plus-circle"></i> Agregar Regla
                    </button>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <strong>ℹ️ ¿Cómo funcionan las reglas?</strong>
                        <ul class="mb-0 mt-2">
                            <li><strong>Rubro:</strong> Ej: "FARMACIA" → Aplica a todos los productos de ese rubro</li>
                            <li><strong>Familia:</strong> Ej: "ANTIBIOTICOS" → Más específico que rubro</li>
                            <li><strong>Marca:</strong> Ej: "BAYER" → Aplica solo a productos de esa marca</li>
                            <li><strong>Producto ID:</strong> Ej: "123" → Aplica solo a ese producto específico (máxima
                                prioridad)</li>
                        </ul>
                        <p class="mb-0 mt-2"><em>Si un producto coincide con varias reglas, se usa la más
                                específica.</em></p>
                    </div>

                    {{#if reglas.length}}
                    <div class="table-responsive">
                        <table class="table table-sm table-striped table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>Rubro</th>
                                    <th>Familia</th>
                                    <th>Marca</th>
                                    <th>Producto ID</th>
                                    <th>Descuento</th>
                                    <th style="width: 100px;">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each reglas}}
                                <tr>
                                    <td>
                                        {{#if this.rubro}}
                                        <span class="badge bg-primary">{{this.rubro}}</span>
                                        {{else}}
                                        <span class="text-muted">—</span>
                                        {{/if}}
                                    </td>
                                    <td>
                                        {{#if this.familia}}
                                        <span class="badge bg-info">{{this.familia}}</span>
                                        {{else}}
                                        <span class="text-muted">—</span>
                                        {{/if}}
                                    </td>
                                    <td>
                                        {{#if this.marca}}
                                        <span class="badge bg-secondary">{{this.marca}}</span>
                                        {{else}}
                                        <span class="text-muted">—</span>
                                        {{/if}}
                                    </td>
                                    <td>
                                        {{#if this.productoId}}
                                        <span class="badge bg-dark">#{{this.productoId}}</span>
                                        {{else}}
                                        <span class="text-muted">—</span>
                                        {{/if}}
                                    </td>
                                    <td>
                                        <strong class="text-success">{{formatPercent this.descuento}}%</strong>
                                    </td>
                                    <td>
                                        <form method="POST"
                                            action="/admin/condiciones/reglas/{{this.id}}?_method=DELETE"
                                            style="display: inline;"
                                            onsubmit="return confirm('¿Eliminar esta regla?');">
                                            <button type="submit" class="btn btn-outline-danger btn-sm"
                                                title="Eliminar">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                    {{else}}
                    <p class="text-muted text-center py-4">
                        <i class="bi bi-info-circle"></i> No hay reglas definidas. Haz clic en "Agregar Regla" para
                        crear la primera.
                    </p>
                    {{/if}}
                </div>
            </div>
            {{/if}}
        </div>
    </div>
</div>

{{! Modal para agregar regla }}
{{#if condicion.id}}
<div class="modal fade" id="modalNuevaRegla" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Agregar Regla de Descuento</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form method="POST" action="/admin/condiciones/{{condicion.id}}/reglas">
                <div class="modal-body">
                    <div class="alert alert-warning">
                        <strong>⚠️ Importante:</strong> Deja en blanco los campos que no desees especificar. Por
                        ejemplo, para aplicar a toda una marca, completa solo "Marca" y "Descuento".
                    </div>

                    <div class="mb-3">
                        <label for="rubro" class="form-label">Rubro (Opcional)</label>
                        <input type="text" class="form-control" id="rubro" name="rubro"
                            placeholder="Ej: FARMACIA, ALIMENTO" oninput="this.value = this.value.toUpperCase()">
                        <small class="text-muted">Se aplica a todos los productos de este rubro</small>
                    </div>

                    <div class="mb-3">
                        <label for="familia" class="form-label">Familia (Opcional)</label>
                        <input type="text" class="form-control" id="familia" name="familia"
                            placeholder="Ej: ANTIBIOTICOS, ANTIPARASITARIOS"
                            oninput="this.value = this.value.toUpperCase()">
                        <small class="text-muted">Más específico que rubro</small>
                    </div>

                    <div class="mb-3">
                        <label for="marca" class="form-label">Marca (Opcional)</label>
                        <input type="text" class="form-control" id="marca" name="marca" placeholder="Ej: BAYER, PFIZER"
                            oninput="this.value = this.value.toUpperCase()">
                        <small class="text-muted">Solo productos de esta marca</small>
                    </div>

                    <div class="mb-3">
                        <label for="productoId" class="form-label">ID de Producto (Opcional)</label>
                        <input type="number" class="form-control" id="productoId" name="productoId"
                            placeholder="Ej: 123">
                        <small class="text-muted">Para un producto específico (máxima prioridad)</small>
                    </div>

                    <div class="mb-3">
                        <label for="descuento" class="form-label">Descuento (%) *</label>
                        <input type="number" class="form-control" id="descuento" name="descuento" step="0.01" min="0"
                            max="100" placeholder="Ej: 20" required>
                        <small class="text-muted">Porcentaje de descuento (0-100)</small>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i> Guardar Regla
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
{{/if}}